namespace AuthServer.Tests.ArchitectureTest;

internal class RequestProcessorTests
{
    // TODO verify only one class implements IRequestHandler directly, and the rest implements through the abstract class RequestHandler
    // TODO verify a request is registered with one IRequestProcessor
    // TODO verify a request is registered with one IRequestValidator
}