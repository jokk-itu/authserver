namespace AuthServer.Tests.Architecture;

internal class RequestProcessorTests
{
    // TODO verify only one class implements IRequestProcessor directly, and the rest implements through the abstract class RequestProcessor
}